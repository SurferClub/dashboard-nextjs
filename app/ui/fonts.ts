import { Inter } from 'next/font/google';
import { Roboto_Mono } from 'next/font/google'

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
});
export const lusi = Roboto_Mono({
    subsets: ['latin'],
    display: 'swap',
});